import Color from 'colorjs.io';


export type PackageJson = { [key: string]: any }; // Define a more specific type if possible
export type ColorScheme =any;
// export type ColorScheme = Record<string, Record<string, Color>>;
export type BuildFileContent = { [key: string]: any }; // Adjust according to the actual content structure
export type OutputPaths = string[]; // Or define a more complex type if needed
